import React, {PropTypes} from 'react';

export default function VolumesSummary({width, height}) {

  return <div style={{width, height, padding: "20px"}}>
    SUmmary
  </div>

}

VolumesSummary.propTypes = {};

VolumesSummary.contextTypes = {};
